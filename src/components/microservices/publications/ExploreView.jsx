import ExaggeratedWorkInProgress from '@/components/common/utils/WIP';
import React from 'react';

const ExploreView = () => {
    return (
        <ExaggeratedWorkInProgress />
    );
};

export default ExploreView;